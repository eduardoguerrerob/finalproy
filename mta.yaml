_schema-version: "3.2"
ID: finalproy
version: 0.0.1
modules:
- name: finalproy-approuter
  type: approuter.nodejs
  path: finalproy-approuter
  requires:
  - name: finalproy_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: finalproy_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: finalproy_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - hrsapui5-content.zip
      name: hrsapui5
      target-path: resources/
- name: hrsapui5
  type: html5
  path: hrsapui5
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: finalproy_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: finalproy_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
